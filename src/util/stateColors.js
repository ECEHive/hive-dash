import { useColorModeValue } from '@chakra-ui/react';

const stateColors = {
    printing: 'green',
    idle: 'yellow',
    down: 'red',
    queued: 'orange',
    completed: 'green',
    failed: 'red'
};

export default stateColors;
