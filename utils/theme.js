import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#17238A',
        },
        secondary: {
            main: '#FFE600',
        },
    },
    typography: {
        fontFamily: 'rubik, serif',
    },
});